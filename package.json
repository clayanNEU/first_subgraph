{
  "name": "test-first-nfts",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-first-nfts",
    "create-local": "graph create --node http://localhost:8020/ test-first-nfts",
    "remove-local": "graph remove --node http://localhost:8020/ test-first-nfts",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-first-nfts",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.63.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
